# UI Environment Setup Record

## Environment

- Node: `v24.19.0`
- npm: `11.17.0` via `npm.cmd` because PowerShell script execution blocks `npm.ps1`
- Python: `3.14.5`
- .NET SDK: `8.0.423`
- .NET runtimes: 6.0 and 8.0 desktop/runtime packs detected
- Package manager: none for the project; no `package.json` or lockfile exists
- Framework: native WPF/.NET desktop scaffold

## UI UX Pro Max

- Package: `ui-ux-pro-max-cli`
- Installed version: `2.14.1`
- Install command: `npm install -g ui-ux-pro-max-cli`
- Project install command: `uipro init --ai claude`
- Location: `.claude/skills/ui-ux-pro-max/`
- Verification: `uipro --version` returned `2.14.1`
- Design-system query: `engineering desktop software CAD application data dense interface`

## Motion

- React Motion package: not installed
- Reason: this repository is a native WPF desktop project, not React, Next.js, Vite, Electron, Tauri, or WebView React
- Implemented alternative: native WPF opacity/translate transitions for command palette and inspector reveal
- Reduced motion: guarded by `SystemParameters.ClientAreaAnimation`

## 21st

- CLI verified: `npx @21st-dev/cli@latest --help`
- Design context: `.21st/design.json` and `.21st/DESIGN.md`
- MCP config generated by CLI:

```toml
[mcp_servers.21st]
url = "https://21st.dev/api/mcp"
bearer_token_env_var = "API_KEY_21ST"
```

- Connection status: not authenticated. `21st search` returned `Not signed in. Run 21st login or set TWENTYFIRST_TOKEN.`
- Secret handling: no API key or token was stored in source.

## Verification Notes

- `dotnet build CADWorkAssistant.sln` completed successfully after the SDK hydrated/was detected.
- `dotnet test CADWorkAssistant.sln --no-build` exited successfully, but the current solution does not include test projects yet.
- `npx @21st-dev/cli@latest review ... --json` returned zero findings and zero reviewed files; the local reviewer does not appear to analyze WPF XAML files.
- No React package manager lockfile was created.
- No Motion package was added.
- WPF UI files were created with local design tokens and no external UI dependencies.

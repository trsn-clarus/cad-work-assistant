namespace CADWorkAssistant.Integration.Tests;

// AutoCAD가 설치된 환경에서만 의미 있는 테스트를 담는다 (docs/ROADMAP.md Milestone 1부터 실제 테스트 추가).
// 지금은 프로젝트가 solution에 포함되어 dotnet test 대상에 잡히는지만 확인한다.
public class PlaceholderTests
{
    [Fact]
    public void Placeholder_ProjectIsWiredIntoTestRun()
    {
        Assert.True(true);
    }
}
